apply plugin: "com.android.library"

android {
    compileSdkVersion 32
    buildToolsVersion "30.0.3"

    defaultConfig {
        minSdkVersion 11
        targetSdkVersion 32
        versionCode 1
        versionName "1.0"
    }
}

dependencies {}