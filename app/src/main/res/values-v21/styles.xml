<resources>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="dividerVertical">@color/transparent</item>
        <item name="dividerHorizontal">@color/transparent</item>
    </style>

    <style name="CardCommon" parent="CardView.Light">
        <item name="android:clickable">true</item>
        <item name="android:elevation">1dp</item>
        <item name="android:foreground">?selectableItemBackground</item>
        <item name="android:padding">2dp</item>
        <item name="cardBackgroundColor">@color/card</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue_grey</item>
        <item name="colorPrimaryDark">@color/blue_grey_dark</item>
        <item name="colorAccent">@color/orange_dark</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowContentTransitions">true</item>
    </style>

</resources>
